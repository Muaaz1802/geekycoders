# Server
NODE_ENV=development
PORT=5000
API_PREFIX=/api/v1

# MongoDB
MONGODB_URI=mongodb://localhost:27017/geekycoders-resume

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRE=7d

# Optional: AI / external services (for resume parsing, tailoring, etc.)
# OPENAI_API_KEY=
# RESUME_PARSER_API_KEY=
